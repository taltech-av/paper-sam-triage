% Placeholder — the plotted series were produced by checkpoints that have been
% deleted pending re-training on the corrected 4,110-frame annotations. The
% previous version is preserved in git history; restore the coordinates there
% once the new runs are dumped, keeping the axis limits below as a starting
% point only (they were fitted to the old data).
\begin{tikzpicture}
\begin{axis}[
  width=\columnwidth,
  height=5.4cm,
  xlabel={Epoch},
  ylabel={Val mIoU (\%)},
  xmin=8, xmax=92,
  xtick={10,20,30,40,50,60,70,80,90},
  ymin=36, ymax=69,
  ytick={40,45,50,55,60,65},
  grid=both,
  grid style={line width=0.3pt, draw=gray!30},
  tick label style={font=\footnotesize},
  label style={font=\small},
]
\node[align=center, font=\small\itshape, text=gray!70]
  at (axis cs:50,52) {Pending re-training on the\\corrected annotations};
\end{axis}
\end{tikzpicture}
