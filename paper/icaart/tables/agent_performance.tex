% Generated by compare_models.py — do not edit by hand.
\begin{table}[t]
\centering
\caption{
Agreement between agent signals on the 90{,}176 paired masks from the 4{,}110 frames completed by both models.
The reference label is Swin quality \textit{good} ($\alpha \geq \tau_q$ per class); masks without a Swin score are excluded.
Swin is a noisy judge rather than ground truth, but it is identical across runs, providing a fixed comparison for the two VLMs.
Cross-backend BBox agreement is 57.0\%, meaning the two VLMs give the same judgement on just over half of all masks.
}

\label{tab:agent_performance}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lcccc}
\toprule
\textbf{Agent} & \textbf{Acc.} & \textbf{Prec.} & \textbf{Rec.} & \textbf{F1} \\
\midrule
BBox VLM (LLaVA-1.6-34B) & 64.1 & 72.0 & 71.1 & 71.5 \\
BBox VLM (Qwen2.5-VL-72B) & 63.7 & 69.0 & 77.8 & 73.1 \\
\bottomrule
\end{tabular}}
\end{table}
