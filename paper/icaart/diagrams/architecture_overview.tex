\begin{tikzpicture}[
  >=Stealth,
  every node/.style={font=\scriptsize},
  samblock/.style ={draw, rounded corners=2pt, align=center, fill=blue!14,
                    minimum width=2.3cm, minimum height=0.58cm},
  swinblock/.style={draw, rounded corners=2pt, align=center, fill=orange!20,
                    minimum width=2.3cm, minimum height=0.58cm},
  vlmblock/.style ={draw, rounded corners=2pt, align=center, fill=purple!14,
                    minimum width=2.0cm, minimum height=0.58cm},
  triblock/.style ={draw, rounded corners=2pt, align=center, fill=yellow!28,
                    minimum width=2.3cm, minimum height=0.58cm},
  evalblock/.style={draw, rounded corners=2pt, align=center, fill=green!22,
                    minimum width=2.3cm, minimum height=0.58cm},
  clftblock/.style={draw, rounded corners=2pt, align=center, fill=teal!15,
                    minimum width=2.3cm, minimum height=0.58cm},
  arr/.style={->, thick},
  darr/.style={->, dashed, black!40, thick},
  stageLabel/.style={font=\tiny\bfseries, text=black!65},
]

%% ── Top row: parallel prep stages ───────────────────────────────────────────
\node[samblock] (sam) at (2.0, 0)
  {SAM (ViT-H)\\[-2pt]{\tiny box-prompted, ZOD}};

\node[swinblock] (swin_train) at (6.0, 0)
  {CLFTv2-Large (SwinV2-L)\\[-2pt]{\tiny 2{,}319 clean frames}};

%% ── (3) Annotation Refinement ────────────────────────────────────────────────
\node[swinblock] (swin) at (4.0, -1.5)
  {Swin Quality Agent\\[-2pt]{\tiny CLFTv2-Large}};

\node[vlmblock] (llava) at (2.2, -2.65)
  {LLaVA-1.6-34B};

\node[vlmblock] (qwen) at (5.8, -2.65)
  {Qwen2.5-VL-72B};

\node[triblock] (triage) at (4.0, -3.75)
  {Triage Rule\\[-2pt]{\tiny deterministic concordance}};

%% ── (4) Downstream Training ──────────────────────────────────────────────────
\node[evalblock] (eval) at (4.0, -5.0)
  {Evaluation\\[-2pt]{\tiny ZOD + iSEAuto}};

\node[clftblock] (clft) at (4.0, -6.0)
  {CLFTv2-Base (SwinV2-B)\\[-2pt]{\tiny Camera--LiDAR CrossFusion}};

%% ── Stage bounding boxes ─────────────────────────────────────────────────────
\begin{scope}[on background layer]
  \fill[gray!4, draw=black!22, dashed, rounded corners=3pt, line width=0.6pt]
    (0.1, 0.42) rectangle (3.9, -0.42);
  \fill[gray!4, draw=black!22, dashed, rounded corners=3pt, line width=0.6pt]
    (4.1, 0.42) rectangle (7.9, -0.42);
  \fill[gray!4, draw=black!22, dashed, rounded corners=3pt, line width=0.6pt]
    (0.8, -1.02) rectangle (7.2, -4.25);
  \fill[gray!4, draw=black!22, dashed, rounded corners=3pt, line width=0.6pt]
    (1.5, -4.55) rectangle (6.5, -6.45);
\end{scope}

%% Stage labels — placed in the clear gaps between boxes
\node[stageLabel] at (2.0,  0.58) {(1) Mask Generation};
\node[stageLabel] at (6.0,  0.58) {(2) Swin Pre-training};
\node[stageLabel] at (4.0, -0.72) {(3) Annotation Refinement};
%% (4) off-centre to avoid the triage→eval arrow at x=4.0
\node[stageLabel, anchor=west] at (0.85, -4.40) {(4) Downstream Training};

%% ── Arrows ───────────────────────────────────────────────────────────────────
%% (1) pseudo-labels → Swin Quality Agent
\draw[arr] (sam.south) -- (2.0,-1.5) -- (swin.west);

%% (2) checkpoint → Swin Quality Agent
\draw[darr] (swin_train.south) -- (6.0,-1.5)
  -- node[above, font=\tiny]{loads} (swin.east);

%% Swin → VLMs (fork)
\draw[arr] (swin.south) -- ++(0,-0.22) -| (llava.north);
\draw[arr] (swin.south) -- ++(0,-0.22) -| (qwen.north);

%% VLMs → Triage (merge)
\draw[arr] (llava.south) -- ++(0,-0.22) -| (triage.north);
\draw[arr] (qwen.south)  -- ++(0,-0.22) -| (triage.north);

%% (3) → (4)
\draw[arr] (triage.south) -- (eval.north);

%% (4) internal
\draw[arr] (eval) -- (clft);

\end{tikzpicture}
