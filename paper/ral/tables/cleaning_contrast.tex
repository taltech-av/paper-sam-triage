% The controlled human-cleaning experiment. Two configs under
% config/vlm/clftv2-base/cleaning/ in the fusion-training repo, driven by
% slurms/clftv2/run_vlm_campaign.sh cleaning. They differ in exactly one field,
% annotation_path; splits, architecture and hyperparameters are byte-identical.
% The lower block is the result to read — see the caption.
\begin{table}[t]
\centering
\caption{Does human cleaning of the labels change what a model learns? Both arms train on the same
         651 frames and are evaluated on the same held-out 201 against the human labels, differing
         by exactly one variable: whether the masks a human called incorrect are erased. Cleaning
         only ever deletes --- the human set is a strict subset of the raw one --- so no other
         difference can explain a gap. This experiment is separate from Table~\ref{tab:ablation}
         because it is the one comparison in which training-set size can be held fixed. The
         intervention is small, removing 5.3\,\% of the object pixels, so the upper block is
         reported with its interval; it is nonetheless significant ($P(\Delta\leq0)=0.003$,
         10{,}000 paired weather-stratified resamples). The lower block reads each class's IoU
         change against the dose of cleaning it received. Both arms are final.}
\label{tab:cleaning}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lccc}
\toprule
\textbf{Arm} & \textbf{mIoU} & \textbf{fw-IoU} & \textbf{$\Delta$mIoU (95\,\% CI)} \\
\midrule
Raw SAM (uncleaned)   & 36.3 & 56.3 & \textemdash \\
\textbf{Human-cleaned} & \textbf{39.0} & \textbf{57.7} & $\mathbf{+2.5}$ \ [$+0.7$, $+4.0$] \\
\midrule
\multicolumn{4}{l}{\textit{Per class: cleaning dose against IoU change}} \\
\textbf{Class} & \textbf{Px removed} & \textbf{\% of class} & \textbf{$\Delta$IoU} \\
\midrule
Human (cyc.\ + ped.) & 535{,}244 & 20.9 & $+2.4$ \\
Sign                 & 693{,}239 & 12.2 & $+3.4$ \\
Vehicle              & 864{,}715 &  3.5 & $+0.9$ \\
\bottomrule
\end{tabular}}
\end{table}
