% Three configs, config/vlm/clftv2-base/shared/config_gt_{fusion,rgb,lidar}.json,
% driven by slurms/clftv2/run_vlm_campaign.sh modality.
% Unlike Tables II and III this one holds the labels fixed and varies the sensor
% stream, so scoring each arm against the labels all three share is sound here —
% the reference-independence problem in those tables does not arise.
\begin{table}[t]
\centering
\caption{Modality ablation on CLFTv2-Base: what each sensor stream contributes, with annotation
         quality held fixed. All three arms train on the curated clean partition (1{,}521 frames)
         against ZOD's own ground truth and are evaluated on its 565 held-out frames against the same
         labels. Because the annotations are identical across the three arms and only the input
         modality changes, this is the one table in which scoring a model against its own training
         label type carries no bias. \pendingnote}
\label{tab:modality_ablation}
\begin{tabular}{lcccc}
\toprule
\textbf{Modality} & \textbf{Veh.} & \textbf{Sign} & \textbf{Human} & \textbf{mIoU} \\
\midrule
RGB only        & \pending{--} & \pending{--} & \pending{--} & \pending{--} \\
LiDAR only      & \pending{--} & \pending{--} & \pending{--} & \pending{--} \\
CrossFusion     & \pending{--} & \pending{--} & \pending{--} & \pending{--} \\
\bottomrule
\end{tabular}
\end{table}
