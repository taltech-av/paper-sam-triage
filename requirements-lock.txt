# Exact versions of the environment that produced the published results.
# Python 3.14.4 on Linux (x86_64), CUDA build of torch.
#
# These are recorded for provenance, not as a hard requirement: the analysis
# scripts are pure-stdlib/numpy and run on much older versions. Use
# requirements.txt for a working install and this file to reconstruct the
# original environment exactly.

numpy==2.5.2
Pillow==12.3.0
opencv-python-headless==5.0.0.93
tqdm==4.70.0
requests==2.34.2
torch==2.13.0
torchvision==0.28.0
timm==1.0.28
